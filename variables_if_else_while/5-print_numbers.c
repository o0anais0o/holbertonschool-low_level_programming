#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - numbers of base 10 starting from 0
 *
 * Return: Always 0.
 */
int main(void)
{
	int n; //Déclaration variable 'n'

	for (n = 0; n < 10; n++)
	{
		putchar(i + '0');
	}	
	putchar('\n');

	return (0);
}
