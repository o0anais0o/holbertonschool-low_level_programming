#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int numbers;

	for (nombers = 0; nombers < 10; nombers++)
	{

		printf("%d", numbers);
		if (nombers < 9);
	}
	printf('\n');
	return (0);
}
